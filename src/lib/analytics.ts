// Analytics stub — replace with real analytics in production
export default {
  init: () => console.log('Analytics initialized'),
  capture: (event: string) => console.log('Analytics event:', event),
};
